pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Building the CI/CD Pipeline Project"
            }
        }

        stage('Test') {
            steps {
                echo "Testing the application"
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploying the HTML Web Page"
            }
        }

    }
}
